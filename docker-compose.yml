services:
  mongo:
    image: mongo:8.2
    container_name: bellion-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: bellion
      MONGO_INITDB_ROOT_PASSWORD: bellion
      MONGO_INITDB_DATABASE: bellion
    volumes:
      - bellion-mongo-data:/data/db
    healthcheck:
      test:
        [
          "CMD",
          "mongosh",
          "--quiet",
          "--username",
          "bellion",
          "--password",
          "bellion",
          "--authenticationDatabase",
          "admin",
          "--eval",
          "db.adminCommand('ping').ok",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

volumes:
  bellion-mongo-data:
